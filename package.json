{
  "name": "ladyfitness-bremgarten-ch",
  "version": "2.0.0",
  "private": true,
  "description": "Lady Fitness Bremgarten — eine Next.js-App (Marketing, Buchen, Kontakt, Admin). Build → dist/ (standalone).",
  "workspaces": [
    "studio-booking"
  ],
  "scripts": {
    "dev": "npm run dev -w studio-booking",
    "build": "npm run build -w studio-booking && node scripts/build-dist.js && node scripts/build-httpdocs-static.js",
    "build:next": "npm run build -w studio-booking",
    "start": "npm run start -w studio-booking",
    "lint": "npm run lint -w studio-booking",
    "install:all": "npm install",
    "pack": "node scripts/pack-studio-booking-full-deploy.js"
  }
}
