# Lady Fitness Bremgarten

```bash
npm install
# studio-booking/.env aus .env.example
npm run dev
```

`npm run build` → **`dist/`** (Next.js standalone: `server.js`, `.next`, `node_modules`, `public`).

Siehe **`repo2web.json`**.
