# studio-booking

Next.js-App (App Router): Marketing-Routen, `/buchen`, `/kontakt`, `/admin`, APIs. **Datenbank:** Supabase — Schema unter `supabase/migrations/`.

**Einstieg:** siehe [`../README.md`](../README.md) im Projektroot (`npm install` im Root, `npm run dev`).
